import { {{pascalCase name}}Response } from '../interfaces/index.interface'

export const {{pascalCase name}} = async ():{{pascalCase name}}Response => {
  // INSERT LOGIC
  // MAKE ASYNC CALLS
  return Promise.resolve()
}
